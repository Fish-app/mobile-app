import 'package:flutter/cupertino.dart';

class Logo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(
      "Màoyi",
      style: TextStyle(fontFamily: "KaushanScript", fontSize: 70),
    );
  }
}
